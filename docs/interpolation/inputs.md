# `inputs.<name>`

The value the user supplied for a declared input, or its default.
Resolved at render time.

Values always render as strings: `integer` renders decimal (`5432`),
`boolean` renders `true` or `false`.
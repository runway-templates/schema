# yaml-language-server: $schema=../../v1alpha1.json
# violation: minPlan must match a SKU from the platform's plan list.
# "scale-massive-launch" is not a real SKU.
apiVersion: schema.runway.horse/templates/v1alpha1
kind: Template

metadata:
  name: demo
  version: 1.0.0
  displayName: Demo
  description: A demo template.
  category: other

services:
  - name: web
    image: nginx:1.27
    resources:
      cpu: half
      memory: 256megs

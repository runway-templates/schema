# yaml-language-server: $schema=../../v1alpha1.json
# violation: services is required and must contain at least one entry.
apiVersion: schema.runway.horse/templates/v1alpha1
kind: Template

metadata:
  name: demo
  version: 1.0.0
  displayName: Demo
  description: A demo template.
  category: other
